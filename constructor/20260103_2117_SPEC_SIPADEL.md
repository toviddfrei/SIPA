# ARTEFACTO: 20260103_2117_SPEC_SIPADEL.md

```text
PROYECTO SIPA - Sistema de Inteligencia de Perfil Automático
Módulo: SIPAdel | Generado por: SIPAdoc v0.2.5
Fecha: 03/01/2026 21:17
Autor: Daniel Miñana & Gemini
----------------------------------------------------------
DESCRIPCIÓN: Especificación técnica detallada.
```

## ⚙️ LÓGICA DE NEGOCIO

AUDITORÍA DEL FICHERO main.py.

1. Auditoría Técnica de tu main.py
Punto Fuerte: La implementación de NTP por socket es impecable y soberana.

Punto a Mejorar: En la Fase 2, calculas boot_duration y lo lanzas al log, pero el objeto logger solo escribe texto. Necesitamos llamar al PersistenceManager para que ese número entre en sistema.db y se aplique la Regla del 30%.

* El fichero actualmente es este:

```python
# ==========================================================
# PROYECTO SIPA - Sistema de Inteligencia de Perfil Automático
# Archivo: main.py
# Módulo: SIPAbap (Arranque / Ignición)
# Versión: 0.2.5 | Fecha: 03/01/2026
# Autor: Daniel Miñana
# ----------------------------------------------------------
# DESCRIPCIÓN: Orquestador principal. Implementa telemetría 
# de alta visibilidad y certificación de tiempo externo vía 
# socket NTP (sin dependencias externas).
# ==========================================================

import time
import socket
import struct
from core.guard.environment_manager import EnvironmentManager
from core.logger.config_loggers import setup_logger
from interface.splash import SipaSplash
from interface.main_dashboard import MainDashboard

def get_ntp_time(host="pool.ntp.org"):
    """
    Obtiene la hora oficial vía protocolo NTP usando sockets.
    Certifica la hora sin depender de librerías externas como requests.
    """
    port = 123
    buf = 48
    address = (host, port)
    # Mensaje NTP (protocolo estándar)
    msg = '\x1b' + 47 * '\0'
    
    try:
        # Configuración de socket UDP
        client = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        client.settimeout(2.0)
        client.sendto(msg.encode('utf-8'), address)
        data, address = client.recvfrom(buf)
        
        # Desempaquetado del tiempo (segundos desde 1900)
        t = struct.unpack("!12I", data)[10]
        t -= 2208988800 # Ajuste a Unix Epoch (1970)
        
        return time.ctime(t)
    except Exception:
        return None

def start_engine():
    """
    Proceso de Ignición con Auditoría de Tiempo:
    1. Certifica la hora mediante socket NTP.
    2. Activa telemetría en memoria.
    3. Valida infraestructura y mide rendimiento.
    """
    start_perf = time.perf_counter()
    
    # 1. Certificación de tiempo (Ente externo vía NTP)
    certified_now = get_ntp_time()
    
    # 2. Activación del sistema de logs
    logger, log_messages = setup_logger()
    
    logger.info("==========================================")
    logger.info(f"SESIÓN: Daniel Miñana")
    if certified_now:
        logger.info(f"TIEMPO NTP CERTIFICADO: {certified_now}")
    else:
        logger.warning("TIEMPO NO CERTIFICADO: Error de conexión NTP.")
    logger.info("==========================================")

    # --- FASE 1: SPLASH & INFRAESTRUCTURA ---
    splash = SipaSplash()
    splash.update_status("SIPA: Verificando integridad...")
    
    guardian = EnvironmentManager()
    
    if guardian.check_all():
        # Volcado de telemetría a consola para auditoría visual
        for msg in log_messages:
            print(f"[*] TELEMETRÍA: {msg}")

        # --- FASE 2: MÉTRICAS ---
        boot_duration = time.perf_counter() - start_perf
        logger.info(f"Arranque validado en {boot_duration:.4f}s")
        
        splash.update_status(f"Sistema Ready: {boot_duration:.2f}s")
        time.sleep(1.5)
        
        # --- FASE 3: LANZAMIENTO ---
        splash.finalize()
        app = MainDashboard()
        app.run()
    else:
        logger.error("Fallo de infraestructura detectado.")
        splash.finalize()

if __name__ == "__main__":
    start_engine()
```

La intención es incluir las últimas modificaciones, mapeo de rutas críticas según el manifiesto y obtener información de telemetría del arranque

## 📊 ESQUEMA DE DATOS

El nuevo fichero será este:

```python
# ==========================================================
# PROYECTO SIPA - Sistema de Inteligencia de Perfil Automático
# Archivo: main.py
# Módulo: SIPAbap (Arranque / Ignición)
# Versión: 0.2.5 | Fecha: 03/01/2026
# Autor: Daniel Miñana
# ----------------------------------------------------------
# DESCRIPCIÓN: Orquestador principal. Certifica tiempo NTP,
# audita infraestructura y persiste métricas de rendimiento
# en la base de datos centralizada (SIPAdel).
# ==========================================================

import time
import socket
import struct
from core.guard.environment_manager import EnvironmentManager
from core.logger.config_loggers import setup_logger
from core.persistence import PersistenceManager  # <--- IMPORTANTE: Vinculación con SIPAdel
from interface.splash import SipaSplash
from interface.main_dashboard import MainDashboard

def get_ntp_time(host="pool.ntp.org"):
    """
    Certificación de hora externa vía protocolo NTP (Capa de Red).
    Garantiza una marca de tiempo inalterable para la auditoría.
    """
    port = 123
    buf = 48
    address = (host, port)
    msg = '\x1b' + 47 * '\0'
    
    try:
        client = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        client.settimeout(2.0)
        client.sendto(msg.encode('utf-8'), address)
        data, address = client.recvfrom(buf)
        t = struct.unpack("!12I", data)[10]
        t -= 2208988800 
        return time.ctime(t)
    except Exception:
        return None

def start_engine():
    """
    FLUJO DE IGNICIÓN SIPA:
    1. Tiempo: Certificación externa.
    2. Infraestructura: Auditoría progresiva del árbol de trabajo.
    3. Persistencia: Registro de métricas y análisis de rendimiento.
    4. Interfaz: Lanzamiento del Dashboard.
    """
    start_perf = time.perf_counter() # Inicio de cronómetro de alta resolución
    
    # --- PASO 1: TIEMPO EXTERNO ---
    certified_now = get_ntp_time()
    logger, log_messages = setup_logger()
    
    logger.info("==========================================")
    logger.info(f"SESIÓN: Daniel Miñana")
    if certified_now:
        logger.info(f"TIEMPO NTP CERTIFICADO: {certified_now}")
    else:
        logger.warning("TIEMPO NO CERTIFICADO: Error de red.")
    logger.info("==========================================")

    # --- PASO 2: AUDITORÍA DE ENTORNO (SIPAbap) ---
    splash = SipaSplash()
    splash.update_status("SIPA: Verificando infraestructura...")
    
    guardian = EnvironmentManager()
    
    if guardian.check_all():
        # --- PASO 3: PERSISTENCIA Y MÉTRICAS (SIPAdel) ---
        db = PersistenceManager()
        if db.connect():
            # Cálculo final de duración de arranque
            boot_duration = time.perf_counter() - start_perf
            
            # Registro en DB y obtención de la Regla del 30%
            estado, media = db.registrar_metrica_arranque(boot_duration)
            
            # Esta línea ahora sí mostrará datos dinámicos en tu consola:
            logger.info(f"RENDIMIENTO: {estado} | Actual: {boot_duration:.4f}s | Media: {media:.4f}s")
            
            # Registro de la sesión en la tabla de sesiones
            db.register_session(version="0.2.5")
            
            splash.update_status(f"Sistema {estado}: {boot_duration:.2f}s")
        
        # Volcado de telemetría acumulada a consola
        for msg in log_messages:
            print(f"[*] TELEMETRÍA: {msg}")

        time.sleep(1.0) # Pausa de seguridad visual
        
        # --- PASO 4: IGNICIÓN DE INTERFAZ ---
        splash.finalize()
        app = MainDashboard()
        app.run()
    else:
        logger.error("FALLO CRÍTICO: Entorno no certificado.")
        splash.finalize()

if __name__ == "__main__":
    start_engine()
```

Realizar el primer commit por fichero main.py ha sido auditado hoy y preparado para commit